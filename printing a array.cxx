#include <iostream>
using namespace std;

int main() {
    // Define an array
    int array[] = {1, 2, 3, 4, 5};

    

    // Print the elements of the array
    cout << "Elements of the array are: ";
    for (int i = 0; i <5 ; ++i) {
        cout << array[i] << " ";
    }
    
    return 0;
}